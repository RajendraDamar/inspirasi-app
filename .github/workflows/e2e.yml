name: disabled-e2e-tests

# This workflow is intentionally a no-op. The repository contains no local or
# CI browser test runners; external MCP-provided browser tools should be used
# for any interactive verification.

on: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "E2E workflow intentionally disabled. Use external MCP browser tools for verification."
